package com.alertnotes.domain.model

/**
 * User-selectable appearance mode, persisted in DataStore and applied
 * app-wide the moment it changes.
 */
enum class ThemeMode {
    SYSTEM,
    LIGHT,
    DARK,
}
